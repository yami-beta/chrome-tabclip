{
  "name": "TabClip",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "watchify -d src/js/background.js -o dist/js/background.js -t [ babelify ] -v",
    "build": "browserify src/js/background.js -o dist/js/background.js -t [ babelify ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yami_beta",
  "license": "MIT",
  "devDependencies": {
    "babel-polyfill": "^6.16.0",
    "babel-preset-latest": "^6.16.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "watchify": "^3.7.0"
  }
}
